services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      FLASK_APP: run.py
      FLASK_RUN_HOST: 0.0.0.0
      FLASK_RUN_PORT: "5000"
    ports:
      - "5000:5000"
    volumes:
      - sqlite_data:/app/instance
volumes:
  sqlite_data:
